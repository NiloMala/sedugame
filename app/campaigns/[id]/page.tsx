'use client';
import Link from 'next/link'; import { Lock, Play } from 'lucide-react'; import { AppShell } from '@/components/shell'; import { useCampaign } from '@/lib/hooks';
export default function CampaignPage({params}:{params:{id:string}}){const {data,error,isLoading}=useCampaign(params.id); return <AppShell>{isLoading?<p>Carregando campanha...</p>:error?<p role="alert">{error.message}</p>:<><p className="font-bold text-brand">CAMPANHA</p><h1 className="mt-1 text-3xl font-bold">{data?.title}</h1><p className="mt-2 text-slate-600">As missões são liberadas em sequência conforme seu progresso.</p><div className="mt-8 max-w-3xl space-y-3">{data?.missions.map((mission)=><article key={mission.id} className="card flex items-center justify-between p-5"><div><p className="text-sm font-bold text-brand">MISSÃO {mission.order}</p><h2 className="mt-1 text-lg font-bold">{mission.title}</h2><p className="mt-1 text-slate-600">{mission.status}</p></div>{mission.locked?<span className="btn-secondary"><Lock className="h-4"/>Bloqueada</span>:<Link className="btn-primary" href={`/play/${mission.id}`}><Play className="h-4"/>Jogar</Link>}</article>)}</div></>}</AppShell>}
